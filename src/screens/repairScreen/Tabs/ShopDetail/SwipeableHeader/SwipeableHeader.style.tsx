import { StyleSheet } from "react-native";

export const swipeableHeadersStyle = StyleSheet.create({
  mainHolder: {
    marginVertical: 12,
    flexDirection: "row",
    alignItems: "center",
    paddingHorizontal: 12,
  },
  panelContent: {
    backgroundColor: "red",
  },
});
