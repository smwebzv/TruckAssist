import { StyleSheet } from "react-native";

export const shortcutsButtonsStyle = StyleSheet.create({
  mainHolder: {
    marginVertical: 12,
    flexDirection: "row",
    alignItems: "center",
    paddingHorizontal: 12,
  },
});
